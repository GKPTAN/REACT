let statusPedido: "pendente" | "enviado" | 0;
statusPedido = 0;
statusPedido = "pendente";
statusPedido = "enviado";

let ids: (string | number)[] = [10, 5, 0, 4, 2, "flecha", "função", "anônimo"];

ids.push(7);
ids.push("Estado");